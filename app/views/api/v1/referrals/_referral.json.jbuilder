json.id referral.id
json.email referral.email
json.status referral.status
json.created_at referral.created_at.strftime("%B %d, %Y at %I:%M %p")
json.updated_at referral.updated_at.strftime("%B %d, %Y at %I:%M %p")
